-- Lean 4 stub (автоматически сгенерировано, требует доработки)
-- Язык: L = {aⁿbⁿ | n≥0} ∪ {aⁿcⁿ | n≥0}. Is this language LL(k) for some k?
-- Вердикт: не LL(k) ни для какого k
-- Метод: substitution

import Mathlib

-- TODO: определить язык L как формальный язык над конечным алфавитом
-- TODO: доказать, что L не является LL(k) ни для какого k
-- Подход: метод подстановки (substitution method)
--   Пусть G — произвольная КС-грамматика порождающая L.
--   Заменить нетерминал подходящим языком и получить противоречие.

-- TODO: formalize not-LL proof
theorem language_not_ll : True := by
  sorry
